# 📄 Project Setup Guide (Google Doc + Apps Script)

এই প্রজেক্টের কাজ হলো:
ওয়েবসাইট থেকে পাঠানো মেসেজ সরাসরি একটি **Google Doc**-এ সেভ করা।

এখানে ২টি মূল ফাইল আছে:

* `index.html`
* `app.js` (বা Apps Script কোড)

নিচের ধাপগুলো **ক্রম অনুযায়ী** ফলো করলেই প্রজেক্ট কাজ করবে।

---

## Step 1: Google Doc তৈরি এবং ID সংগ্রহ

1. একটি নতুন Google Doc খুলুন।

2. ব্রাউজারের অ্যাড্রেস বারে দেখবেন এমন লিংক:

   ```
   https://docs.google.com/document/d/THIS_IS_THE_ID/edit
   ```

3. `d/` আর `/edit` এর মাঝখানের অংশটাই হলো আপনার **Doc ID**
   সেটি কপি করে রাখুন।

---

## Step 2: Google Apps Script সেটআপ

1. আপনার Google Doc খুলে যান:

   ```
   Extensions > Apps Script
   ```

2. সেখানে আগের কোড মুছে দিয়ে
   দেওয়া `apps_script.txt` বা Apps Script কোড পেস্ট করুন।

3. এই লাইনে:

   ```js
   var docId = 'YOUR_DOC_ID_HERE';
   ```

   এখানে `YOUR_DOC_ID_HERE` এর জায়গায় আপনার কপি করা Doc ID বসান।

4. ফাইলটি **Save (Ctrl+S)** করুন।

---

## Step 3: Web App হিসেবে Deploy করা (সবচেয়ে গুরুত্বপূর্ণ)

1. উপরে ডানদিকে ক্লিক করুন:

   ```
   Deploy > New deployment
   ```

2. Type সিলেক্ট করুন:
   **Web App**

3. Settings:

   * Execute as: **Me**
   * Who has access: **Anyone**

4. Deploy ক্লিক করুন।

5. প্রথমবার হলে Permission চাইবে:

   * Review Permissions
   * আপনার Gmail সিলেক্ট করুন
   * Advanced > Go to project (unsafe)
   * Allow

6. শেষে আপনি একটি **Web App URL** পাবেন
   সেটি কপি করে রাখুন।

---

## Step 4: app.js ফাইলের সাথে Sync করা

1. `app.js` ফাইল খুলুন।

2. একদম শুরুতে দেখবেন:

   ```js
   const SCRIPT_URL = '...';
   ```

3. `'...'` এর জায়গায় আপনার কপি করা **Web App URL** বসান।

4. ফাইল সেভ করুন।

---

## Step 5: প্রজেক্ট রান করা

1. `index.html` ফাইল ব্রাউজারে ওপেন করুন।
2. মেসেজ পাঠান।
3. মেসেজটি সরাসরি আপনার **Google Doc**-এ চলে যাবে ✅

---

## ⚠️ Important Note (খুব জরুরি)

যখনই আপনি **Apps Script কোডে কোনো পরিবর্তন করবেন**:

* আবার নতুন করে করতে হবে:

  ```
  Deploy > New Deployment
  ```
* নতুন একটি URL পাবেন
* সেই নতুন URL আবার `app.js`-এ বসাতে হবে

না হলে পুরনো ভার্সনই চলবে।

---

## Flow Diagram (সহজভাবে)

```
index.html
     |
     v
  app.js (fetch)
     |
     v
Apps Script Web App
     |
     v
Google Doc
```

---

## Common Problems

### 1. Data যাচ্ছে না?

* Web App URL ঠিক আছে কিনা দেখো
* Execute as = Me আছে কিনা চেক করো
* Anyone access দেওয়া আছে কিনা দেখো

### 2. Permission Error?

* আবার Deploy করো
* Allow ঠিকভাবে দিয়েছো কিনা দেখো

---

## Use Case

এই সিস্টেম ব্যবহার করতে পারো:

* Contact Form
* Anonymous Message System
* Feedback Collector
* Simple Chat Logger

সব ডেটা থাকবে **আপনার নিজের Google Doc-এ** —
কোনো database দরকার নেই 🔥
